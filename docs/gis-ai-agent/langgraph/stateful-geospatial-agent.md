# Stateful geospatial agent (LangGraph)

**Status: Coming soon**

This page is reserved for a LangGraph tutorial that walks through a **stateful, multi-step GIS pipeline** using GIS MCP Server as the MCP tool backend.

## Intended scope (not implemented yet)

- Connect LangGraph to GIS MCP over HTTP (`/mcp`)
- Model a graph: inspect inputs → CRS checks → analysis → save outputs
- Optional checkpoint / resume patterns provided by LangGraph (not by GIS MCP)

Until this guide ships, use:

- [LangChain basic geospatial agent](../langchain/basic-geospatial-agent.md)
- [Best practices for GIS agents](../best-practices.md)
- [LangGraph section overview](README.md)
